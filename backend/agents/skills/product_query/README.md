## Product Query Skill

该 skill 用于“产品查询/榜单/筛选/推荐”类问题的数据获取。

- **有基金代码（6 位数字）**：复用 `product_compare` 的聚合取数逻辑，返回单/多基金的结构化数据。
- **无基金代码**：使用 AkShare 的开放式基金排行接口拉取候选集，并结合“盈亏概率/平均收益”作为稳健性参考，输出 TopN 结果供上层 LLM 生成自然语言回答。

