## Agent skills

### Issue 追踪

本仓库的 issue 记录在 GitHub Issues（`github` remote：yds123-div/fpai），所有操作使用 `gh` CLI。详见 `docs/agents/issue-tracker.md`。

### 领域文档

单上下文：根目录一个 `CONTEXT.md` + `docs/adr/` 目录。详见 `docs/agents/domain.md`。
